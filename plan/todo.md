# 완료/잔여 TODO 검토

> 마지막 갱신: 2026-07-14
> 현재 기준: 오래된 구현 TODO는 실제 코드/문서 기준으로 완료 여부를 보정하고, 오픈 전 남은 작업은 하단 `오픈 전 마무리` 항목에서 관리한다.

### 연혁
[x] 제목 필드 불필요. 숨김 처리

### Footer
[x] Footer → 센터 정보로 이름 변경
[x] 'Footer 메뉴' 필드 제거

### 커리큘럼
[x] 슬러그 필드 추가 및 슬러그 정제

### 하이틴센터 특강
[x] 슬러그 정리 : highteen-special-class-19 → class-19

### 캐스팅 출연현황
[x] 슬러그 추가 및 슬러그 정제

### 오디션 일정
[x] 일정 유형 디폴트 : 선택해 주세요.
[x] 시작일 지정 시 해당 값이 종료일로 자동 적용되도록 수정

### 다이렉트 캐스팅
[x] 유효성 검사 알림 워딩(Something went wrong)과 필드 색상(빨간색 테두리) 미노출됨

### 드라마/광고 출연장면
[x] 슬러그 추가 및 슬러그 정제 : {center}-{작품명}
[x] 센터 선택 필드 색상(빨간색 테두리) 미노출됨

### 출신 아티스트
[x] 소속사 필수 입력값으로 수정

### 소속사 로고설정
[x] 소속사 로고 이미지 필수 입력 값으로 수정

### 합격후기
[x] 슬러그 정리 : exam-passed-review-330 → review-330

### 합격결과(센터)
[x] '미디어' 필드 명 '대표 이미지'로 변경

### FAQ
[x] 센터를 질문 아래로 위치하고 센터 값이 2개 이상일 때만 센터별 답변을 선택할 수 있어야할 것 같아. 센터가 1개인데도 센터별 답변으로 선택되는 경우가 있는데 이건 모두 공통으로 쓰는게 맞지 않나. 그리고 명칭이 공통이 아니라 '단일'로 변경해서 혼선을 줄여야할 것 같아.
[x] 분류 필수값인데 필드 색상(빨간색 테두리) 미노출됨
[x] 모든 게시글 상태 값 공개로 변경.

### 스타카드
[x] 이미지 필드 1개는 필수.
[x] 센터 선택 디폴트 값이 ALL

### 커리큘럼
[x] 키즈 센터는 현재 커리큘럼이 없기 때문에 키즈센터 제거
[x] 아트센터와 입시센터, 하이틴가 중복으로 들어간 강의는 아트센터 제거.
[x] 센터별로 클래스가 달라서 센터 중복 선택을 할 수 없어서 단일 선택으로 바꾸고 우측 보조 패널에서 제거 후 '강의 정보' 탭에서 가장 먼저 선택할 수 있게 위치 변경.
[x] 하이틴 센터 클래스 : 입문 I CLASS, 중급 R CLASS, 심화 U CLASS, 전문 DA CLASS
[x] 입시센터 클래스 : 입시반, 입시예비반, 예고입시반

## 메인
- [x] 글로벌 그룹/컬렉션의 표시 이름을 메인 설정 흐름으로 정리.
  - 구현 기준: `main` 글로벌, `main-banners` 컬렉션, `main-statistics` 글로벌은 Payload 관리자 `메인설정` 그룹에 위치한다.
- [x] 상세 구현 플랜 보정.
  - 완료/후속 기준: [메인-배너-구현-플랜.md](./메인-배너-구현-플랜.md)

- [x] 메인 배너 등록.
  - 필수/선택 필드: 제목, 타이틀, 설명, 센터, 상태, 예약 노출, 데스크톱 이미지, 모바일 이미지, 데스크톱 영상, 모바일 영상.
  - 미디어 규칙: 데스크톱 이미지는 필수이고, 모바일 이미지는 데스크톱 이미지 fallback을 사용한다. 영상이 있으면 영상 우선, 이미지는 poster 역할을 한다.
  - 연결 콘텐츠: 입시센터 외 센터는 프로필, 입시센터는 대학교 그룹별 합격후기를 연결한다.
  - 노출 순서: `main` 글로벌의 센터별 배너 순서 배열에서 관리한다.

## SNS
- [x] 센터 메인 SNS 섹션 구현.
  - 구현 기준: `social-links` 컬렉션은 Payload 관리자 `메인설정` 그룹에 위치한다.
  - 필드 기준: 관리자 제목, 센터, SNS 타입, SNS 링크, 대표 이미지, 상태.
  - 인스타그램은 대표 이미지가 필수이고, 유튜브는 링크에서 썸네일을 가져오는 fallback을 사용한다.
  - 공개 노출: 센터 메인에서 최신 공개 SNS 링크를 외부 URL 새 창 링크로 노출한다.

## 오픈 전 마무리

- [x] 고용량 이미지 업데이트 정책 수립.
  - 관리자 업로드 이미지는 공개용 운영본 기준으로 2MB 이하만 허용한다.
  - 메인 배너 영상은 데스크톱 20MB 이하, 모바일 10MB 이하를 권장하고 필드 안내로 고지한다.
  - 원본 보관이 필요하면 공개 R2 `media/`가 아니라 별도 보관소에서 관리한다.
  - 세부 기준: [R2 미디어 업로드 현황](../docs/08-R2-미디어-업로드-현황.md)
- [ ] 남은 media role prefix 정규화.
  - 대상: `main_banners`, `social_links`, `classrooms_rels`의 `media/assets/{mediaId}` 참조.
  - 방식: R2 object를 role prefix로 복사한 뒤 `media.prefix`를 갱신하고, 기존 object 삭제는 별도 stale/orphan 정리로 분리한다.
- [ ] 실제 도메인 연결.
  - 대표 도메인/canonical 기준을 확정한다.
  - Vercel 도메인, 기존 센터별 도메인, Cloudflare R2 공개 도메인, `NEXT_PUBLIC_SITE_URL`, `R2_PUBLIC_BASE_URL` 전환 순서를 정한다.
  - 연결 후 sitemap, canonical, redirect, 센터 메인, Payload admin, media 공개 URL을 점검한다.
- [ ] 인수인계 자료 생성.
  - Vercel, Neon, Cloudflare R2, Payload admin 계정/권한 기준을 정리한다.
  - 배포, DB 복구, R2 미디어, 관리자 콘텐츠 수정, 문의/상담 확인, 이미지 업데이트 정책을 운영자용 문서로 묶는다.

## 보안 및 코드 사용성 개선

> 기준: 위험도가 높고 수정량이 작은 항목을 먼저 처리한다. 각 단계는 가능한 한 별도 작업 단위로 완료하고, 다음 단계로 넘어가기 전에 관련 회귀 테스트와 `npm run typecheck`, `npm run lint`를 통과시킨다.
>
> 작업량 표기: `XS`는 한두 파일의 단순 제한, `S`는 관련 테스트를 포함한 작은 변경, `M`은 여러 컬렉션 또는 운영 설정을 함께 확인해야 하는 변경이다.

### 1단계 — 범용 일괄 수정 응답의 숨김 필드 노출 차단 (`P0`, `S`)

- [x] 클라이언트가 보낸 `showHiddenFields`를 일괄 수정 파라미터로 받지 않는다.
- [x] 수정 후 검증 조회의 `overrideAccess: true`를 제거하고 요청 사용자 권한을 그대로 적용한다.
- [x] 응답 문서도 접근 제어가 적용된 결과만 반환한다.
- [x] `users`, `inquiries`처럼 민감 필드가 있는 컬렉션의 적용 범위를 검토한다.
  - 공통 엔드포인트에서 숨김 필드 요청을 제거하고 모든 작업·검증 조회에 사용자 접근 제어를 적용하므로, 기존 일괄 작업 UX를 유지하는 쪽을 선택했다.
- [x] 일반 관리자/센터 관리자가 `hash`, `salt`, 비밀번호 재설정 토큰 등 숨김 인증 필드를 응답으로 받을 수 없음을 테스트한다.

완료 기준: 권한이 낮은 사용자가 자신의 계정을 일괄 수정해도 숨김 필드가 응답에 포함되지 않고, 기존 일괄 수정 기능은 정상 동작한다.

### 2단계 — 관리자 이미지 삭제 범위를 전용 prefix로 제한 (`P1`, `XS`)

- [x] `/api/admin-images`의 삭제 요청은 최종 R2 key가 반드시 `admin-images/`로 시작할 때만 허용한다.
- [x] 같은 공개 R2 도메인의 `media/`, `inquiries/attachments/` 등 다른 경로는 삭제 요청을 거부한다.
- [x] 정상 삭제, 다른 prefix 거부, 다른 origin 거부 테스트를 추가한다.

완료 기준: 로그인한 사용자도 관리자 이미지 경로 밖의 R2 객체는 이 API로 삭제할 수 없다.

### 3단계 — 운영 Payload 시크릿 fallback 제거 (`P1`, `XS`)

- [x] 운영 환경에서 `PAYLOAD_SECRET`이 없으면 서버 시작 단계에서 명확히 실패시킨다.
- [x] 예측 가능한 개발용 fallback은 로컬 개발 환경에서만 허용한다.
- [x] Vercel Production/Preview 환경에 서로 적절한 시크릿이 설정되어 있는지 확인한다.
  - 기존 Production 시크릿은 유지하고, 누락되어 있던 Preview에는 별도의 랜덤 시크릿을 추가했다.

완료 기준: 운영 환경은 하드코딩된 기본 시크릿으로 기동할 수 없고, 로컬 개발 흐름은 유지된다.

### 4단계 — 상담 첨부파일 처리 정책 결정 및 현재 노출 해소 (`P1`, `S~M`)

- [x] 현재 데이터 규모를 읽기 전용으로 확인한다.
  - 2026-07-14 기준, 현재 `.env.local` 대상 원격 DB: 문의 18건 중 첨부 URL/R2 key 보유 2건.
  - 같은 원격 R2의 `inquiries/attachments/`: 객체 2개, 총 988,248바이트.
- [x] 기존 첨부 2건이 보존이 필요한 실제 자료인지 확인한다.
  - 2건 모두 실제 제휴 문의에 첨부된 JPG/PNG 자료이므로 기능을 제거하지 않고 보존·비공개 이전하는 방향으로 결정했다.
- [x] 첨부 기능이 필요하지 않다면 다음의 작은 변경으로 우선 차단한다.
  - 공개 상담 폼에서 첨부 입력을 제거한다.
  - 서버에서도 첨부가 포함된 요청을 거부해 우회 업로드를 막는다.
  - 기존 2개 파일은 안전하게 인계/보관한 뒤, 사용자 확인을 받고 공개 R2 객체와 DB URL을 정리한다.
  - 검토 결과 적용하지 않는 분기로 확정했다.
- [x] 첨부 기능이 필요하다면 별도 비공개 R2 bucket 또는 비공개 저장 경로로 이동하고, 관리자 인증 다운로드나 짧은 만료시간의 서명 URL을 사용한다.
  - `baewoo-private` 비공개 bucket을 생성하고, 신규 업로드·관리자 인증 다운로드·기존 자료 이전 코드를 구현했다.
  - bucket 전용 S3 API token을 설정하고 기존 2건을 복사했으며, 비공개/공개 객체 크기 일치와 DB 공개 URL 0건을 확인했다.
  - Production에서 관리자 인증 다운로드를 확인한 뒤 사용자 승인 하에 공개 원본 2개를 삭제했다.
  - 삭제 후 비공개 객체 2개 읽기 성공, 공개 객체 2개 미존재, DB 공개 URL 0건을 재확인했다.
- [x] 첨부 기능을 유지할 경우 Vercel 요청 본문 제한보다 큰 현재 10MB 허용값도 함께 조정한다.
  - 공개 폼과 서버 검증을 모두 4MB로 통일했다.

완료 기준: 개인정보 첨부파일을 영구 공개 URL로 새로 저장하지 않으며, 기존 2건의 공개 노출도 해소된다.

### 5단계 — 공개 API의 초안·보관 콘텐츠 접근 차단 (`P1`, `M`)

- [x] 비로그인 읽기 조건에 커스텀 `displayStatus: published` 필터를 공통 적용한다.
  - 센터 범위 컬렉션은 `centerScopedPublishedCollectionAccess`를 사용하고, 로그인 사용자는 기존 센터 범위 조건을 유지한다.
  - 필드명이 다른 `main-banners.status`와 단일 센터 구조인 `social-links`는 기존 권한 함수 안에서 비로그인 조건만 제한했다.
- [x] `main-banners`, `news`, `artist-press`, `faqs`, `star-cards`, `agencies`, `highteen-special-classes`, `exam-passed-reviews`, `exam-results`, `social-links`, `casting-directors`와 같은 상태 필드 사용 컬렉션을 일괄 감사한다.
  - Payload에 등록된 상태 필드 컬렉션 12개를 확인했으며, `direct-castings`는 기존에도 비로그인 읽기를 전면 거부하므로 더 넓은 공개 권한을 새로 열지 않았다.
- [x] 공개 REST/GraphQL 요청에서 `draft`, `archived` 문서를 직접 조회할 수 없는지 테스트한다.
  - 로컬 데이터의 뉴스 초안 361건·보관 1건을 기준으로 REST 초안 조건 조회는 0건, 보관 문서 ID 직접 조회는 404, 공개 조건 조회는 정상 반환됨을 확인했다.
  - 현재 프로젝트는 GraphQL 라우트를 등록하지 않아 `/api/graphql`과 `/graphql` 모두 404임을 확인했다.
- [x] 로그인 관리자와 센터 관리자의 기존 관리 권한은 유지되는지 확인한다.
  - master는 전체 읽기, 센터 manager는 기존 센터 및 `all` 범위, `social-links`는 해당 단일 센터 범위, `main-banners`는 기존 전체 읽기가 유지되는지 권한 테스트로 확인했다.

완료 기준: 프론트엔드 쿼리뿐 아니라 Payload API 자체에서도 미공개 콘텐츠가 차단된다.

### 6단계 — 사용하지 않는 API와 과도한 요청 표면 축소 (`P2`, `S~M`)

- [x] 앱에서 GraphQL 사용 여부를 다시 확인하고, 사용하지 않으면 비활성화한다.
  - 소스 전체에서 GraphQL 요청과 전용 라우트 등록이 없고, Payload catch-all에는 REST 핸들러만 연결되어 있음을 확인했다.
  - 로컬 `/api/graphql` GET/POST가 모두 404이며, 별도 GraphQL 인터페이스를 새로 등록하지 않는 현재 구성을 유지한다.
  - 직접 `graphql` 의존성의 정리는 Payload 패키지 업데이트와 함께 9단계에서 별도로 판단한다.
- [x] Payload `maxDepth`를 실제 화면에 필요한 최소 수준으로 제한한다.
  - 프론트와 관리자 쿼리의 최대 사용값 `depth: 3`을 기준으로 전역 `maxDepth: 3`을 적용했다.
  - REST `depth=10` 요청 결과가 `depth=3`과 동일하게 제한됨을 로컬 API에서 확인했다.
- [x] 상담/Turnstile 및 비용이 큰 공개 API에 IP·사용자 기준 rate limit을 적용한다.
  - 상담은 IP당 10분 5회, Turnstile 검증은 IP당 1분 20회, 캐스팅 현황 추가 조회는 IP당 1분 60회로 제한했다.
  - Vercel/Cloudflare 전달 IP를 우선 사용하고 식별자는 Payload 시크릿 기반 HMAC-SHA-256으로만 저장한다. 공통 헬퍼는 로그인 사용자 ID가 주어지면 IP보다 사용자 기준을 우선한다.
  - 서버리스 인스턴스 간에도 공유되도록 기존 PostgreSQL `payload_kv`에 원자적으로 카운트하며, 별도 스키마 변경은 만들지 않았다.
  - 동시 20회 요청에서 제한 10회만 허용되고 나머지 10회가 차단되며, 실제 라우트가 한도 초과 시 429와 `Retry-After`를 반환함을 확인했다.
- [x] Vercel/Cloudflare 설정과 중복 여부를 확인한 뒤 CSP 등 보안 헤더를 코드 또는 edge 설정의 한 곳에서 관리한다.
  - 2026-07-14 기준 Vercel Firewall은 커스텀 규칙 미설정, Attack Mode 꺼짐, System Mitigations 활성 상태여서 애플리케이션 rate limit과 중복되는 규칙이 없다.
  - 실제 도메인은 Vercel에서 HSTS만 제공하고 CSP 등은 없었으며, Cloudflare proxy 응답 헤더도 확인되지 않았다.
  - CSP, Permissions Policy, Referrer Policy, nosniff, frame 차단은 `config/security-headers.mjs` 한 곳에서 정의해 Next.js 전체 경로에 적용했다. HSTS는 기존 Vercel 플랫폼 설정을 유지한다.
  - 상담 Turnstile, 네이버 지도, Payload 관리자 화면을 브라우저로 확인해 CSP 차단 회귀가 없음을 검증했다.

완료 기준: 사용하지 않는 인터페이스와 비정상적으로 깊거나 반복적인 요청이 기본값으로 열려 있지 않다.

### 7단계 — 업로드 검증 강화 (`P2`, `S~M`)

- [ ] 관리자 이미지의 SVG 허용 필요성을 확인하고, 불필요하면 허용 목록에서 제거한다.
- [ ] 확장자와 브라우저 제공 MIME만 신뢰하지 않고 실제 파일 시그니처를 확인한다.
- [ ] 상담 첨부를 유지한다면 악성 문서 검사 또는 허용 형식 축소 정책을 정한다.
- [ ] 업로드 크기 제한을 애플리케이션, Vercel, R2 전송 방식 사이에서 일치시킨다.

완료 기준: 확장자/MIME 위장 파일과 실행 가능한 SVG가 단순 검사를 통과하지 못한다.

### 8단계 — 테스트 실행 경로와 CI 복구 (`코드 사용성`, `M`)

- [ ] `package.json`에 전체 테스트를 재현 가능하게 실행하는 `test` 스크립트를 추가한다.
- [ ] 현재 실패하는 테스트 4건을 구현 변경인지 기대값 노후화인지 판별해 수정한다.
  - `AdminArrayRowLabels.test.ts`
  - `AdminTodoFields.test.ts`
  - `CastingAppearances.test.ts`
  - `adminListSelectOptimization.test.ts`
- [ ] CI에서 최소 `test`, `typecheck`, `lint`를 실행한다.

완료 기준: 로컬과 CI가 같은 명령으로 전체 테스트를 실행하고 모두 통과한다.

### 9단계 — 의존성 취약점과 실행 의존성 정리 (`P2`, `M`)

- [ ] `vercel`, `jsdom`처럼 운영 런타임 import가 없는 패키지는 `devDependencies` 이동 여부를 확인한다.
- [ ] Payload 패키지군을 같은 버전으로 소규모 업데이트한 뒤 `npm audit --omit=dev` 결과를 다시 확인한다.
- [ ] 자동 강제 업데이트 대신 직접 의존성부터 나누어 올리고 각 단계마다 타입·테스트·빌드를 검증한다.

완료 기준: 실제 운영 의존성 기준 audit 결과가 줄고, CLI/테스트 도구가 운영 번들 의존성으로 분류되지 않는다.

### 10단계 — 오류 은폐와 대형 컴포넌트 개선 (`코드 사용성`, `M`, 점진 진행)

- [ ] Payload 조회 실패를 빈 목록/404로 바꾸는 catch 패턴을 감사하고, 일시적 DB 오류와 실제 빈 데이터를 구분한다.
- [ ] FAQ처럼 빈 결과가 캐시에 저장될 수 있는 경로부터 수정한다.
- [ ] `ConsultationForm.tsx`, `GradeSystemTabs.client.tsx`, `CenterHomeSections.tsx`, `BannerSlider.client.tsx`는 기능 변경 시 관련 단위만 추출하며 점진적으로 분리한다.
- [ ] 대규모 선행 리팩터링은 하지 않고, 실제 수정이 필요한 흐름부터 테스트 가능한 경계로 나눈다.

완료 기준: 장애가 정상적인 빈 화면으로 장시간 캐시되지 않고, 주요 대형 파일의 변경 범위가 점차 좁아진다.

### 11단계 — 운영 문서와 개인정보 보유주기 정합성 (`코드 사용성/운영`, `S~M`)

- [ ] README의 현재 라우트, Payload 컬렉션, 로컬 절대경로를 실제 구현과 맞춘다.
- [ ] 개인정보처리방침의 문의/첨부파일 3년 보유 문구에 대응하는 정기 삭제 작업 또는 운영 절차를 만든다.
- [ ] 보안 설정, 비공개 첨부 다운로드, 의존성 점검 절차를 인수인계 문서에 포함한다.

완료 기준: 문서에 적힌 보안·보유 정책과 실제 코드 및 운영 절차가 일치한다.
