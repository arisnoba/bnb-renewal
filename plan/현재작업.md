# 현재 작업 기록

> 기준 문서:
> - [docs/03-Payload-admin-운영-UX.md](../docs/03-Payload-admin-운영-UX.md)
> - [docs/00-문서-인덱스.md](../docs/00-문서-인덱스.md)
>
> 마지막 갱신: 2026-04-28
> 현재 진행 기준: 공식 Payload `website` 템플릿의 관리자 작성 UX를 기존 컬렉션에 선택적으로 이식한다.

## 현재 상태

- `dev` 브랜치에서 공식 Payload `website` 템플릿 기반 프론트와 공통 컴포넌트가 1차로 들어왔다.
- 기존 Payload admin/API/Postgres 구성은 유지했다.
- 신규 `media`, `pages`, `posts`, `categories`, `header`, `footer` 모델이 추가되어 있다.
- 실제 운영 데이터 모델로는 `pages`, `posts`를 쓰지 않을 계획이다.
- 앞으로는 `pages/posts` 컬렉션 자체보다 템플릿의 필드 구성, 관리자 UI, SEO, media, validation, slug UX를 기존 컬렉션에 이식한다.
- 1차 구현 우선순위는 `news`가 가장 높다.
- DB migration은 아직 생성하지 않았다. 기존 스키마 diff 질문이 먼저 떠서 자동 선택하지 않았다.

## 작업 원칙

- 기존 레거시 필드는 삭제하지 않는다.
- 신규 작성 UX 필드는 optional로 추가한다.
- 프론트는 신규 필드 우선, 레거시 필드 fallback 구조로 간다.
- 한 번에 모든 컬렉션을 바꾸지 않는다.
- 대표 컬렉션 1개에서 패턴을 검증한 뒤 확장한다.
- migration 생성 전 기존 schema diff를 먼저 판단한다.
- seed 라우트처럼 destructive 가능성이 있는 템플릿 기능은 활성화하지 않는다.
- 로컬 Postgres/Payload DB에는 레거시 원본 컬럼을 그대로 늘리지 않고, 운영 필드와 최소 원본 추적 필드만 분리해서 둔다.

## 완료

- [x] `dev` 브랜치 생성
- [x] 공식 Payload `website` 템플릿 구조 확인
- [x] 기존 `(site)` 프론트 라우트를 템플릿 `(frontend)` 구조로 교체
- [x] 기존 Payload admin/API 라우트 유지
- [x] 템플릿 공통 컴포넌트, 블록, 필드, provider, utility 추가
- [x] `media`, `pages`, `posts`, `categories`, `header`, `footer` 모델 추가
- [x] 템플릿 plugin 의존성 추가
- [x] destructive seed route 제거
- [x] destructive seed 구현 파일 제거, static homepage fallback만 유지
- [x] migration 전에도 `/`가 뜨도록 homepage/header/footer fallback 추가
- [x] `npm run payload:generate-types`
- [x] `npm run payload:generate-importmap`
- [x] `npm run typecheck`
- [x] `npm run lint`
- [x] `npm run build`
- [x] `/` HTTP 200 확인
- [x] `/admin` HTTP 200 확인
- [x] 관리자 UX 이식 계획 문서 작성
- [x] 컬렉션별 필드 작성 폼 작성

## 현재 우선순위

1. 저장 버튼을 `posts`, `pages` 템플릿처럼 눈에 띄게 개선한다. - 1차 완료
2. 관리자 사이드바 필드를 정리한다. - `news` 1차 완료
   - 발행일
   - 작성자
   - slug
   - 센터 선택
   - 상태
3. Lexical rich text를 순차 적용한다.
   - 뉴스 - 1차 완료
   - 출신 아티스트 - 1차 완료
   - 드라마/광고 출연장면
   - 합격후기

## 다음 결정 필요

- [ ] `pages`, `posts`, `categories`, `header`, `footer` 컬렉션/글로벌을 계속 둘지 제거할지 결정
  - 현재는 템플릿 참조와 빌드 호환을 위해 남아 있다.
  - 운영에서 쓰지 않을 경우 제거하거나 관리자에서 숨기는 방향을 검토한다.
- [ ] `media` 컬렉션은 유지할지 확정
  - 현재 판단으로는 신규 게시글/콘텐츠 작성에 유용하므로 유지 후보.
- [ ] UX 이식 대상 컬렉션 목록 확정
  - 현재 1차 우선순위는 `news`
- [x] 대표 적용 컬렉션 1개 선정
  - 대표 컬렉션: `news`
- [ ] versions 적용 대상 결정
  - 후보: 뉴스, 출신 아티스트, 프로필성 콘텐츠
- [ ] slug 생성 규칙 결정
  - 후보: 날짜/ID 기반 자동 생성 + 수동 수정 허용
- [ ] SEO 탭 적용 대상 결정
  - 후보: 뉴스, 출신 아티스트, 프로필성 콘텐츠

## 인터뷰 진행 방식

긴 작성 폼을 사용자가 직접 전부 채우는 방식은 중단한다. 대신 에이전트가 [인터뷰 노트](../docs/archive/payload/Payload-admin-컬렉션-필드-작성폼.md)를 기준으로 짧은 질문을 하나씩 하고, 답변이 모이면 이 TODO와 구현 계획을 갱신한다.

- [x] 질문 1: `news` 1차 적용 범위를 전체 패턴 검증으로 할지, 저장 버튼/사이드바/Lexical까지만 할지 결정
  - 추천안대로 저장 버튼, 사이드바, Lexical, media, SEO, slug, versions를 `news`에 먼저 적용
- [x] 질문 2: 저장 버튼 개선 범위를 관리자 전체로 할지, 콘텐츠 작성 화면만 할지 결정
  - 관리자 전체 저장 버튼에 CSS 시각 강조 적용
- [x] 질문 3: 사이드바 필드 순서 결정
  - 센터 선택, 상태, 발행일, 작성자, slug 순서 적용
- [x] 질문 4: 신규 Lexical 필드명 결정
  - `body`
- [x] 질문 5: 기존 `bodyHtml` 위치 결정
  - `news` 레거시/원본 탭으로 이동
- [x] 질문 6: versions만 적용할지 draft까지 적용할지 결정
  - `versions: { maxPerDoc: 15 }`만 적용, draft는 보류
- [x] 질문 7: slug 생성 규칙 결정
  - 영문/숫자 토큰 우선, 한글만 있으면 `news-{날짜}-{짧은 suffix}`
- [x] 질문 8: SEO 적용 컬렉션 결정
  - `news` 다음 적용 대상으로 `artist-press`에 SEO 탭과 media fallback을 우선 적용

## 1차 구현 대상: News

> `news`를 최우선으로 적용한다. 저장 버튼, 사이드바, Lexical, media, SEO, slug, versions의 대표 패턴을 여기서 먼저 검증한다.

- [x] `news` 컬렉션을 대표 컬렉션으로 선정
- [x] `title`을 탭 밖 최상단 필드로 유지
- [x] 콘텐츠 탭 구성 확정
  - 후보: `category`, `excerpt`, 신규 `body`
  - 기존: `bodyHtml` 유지
  - 기존 `bodyHtml`은 `body`가 비어 있을 때 Lexical 값으로 자동 변환
- [x] 미디어 탭 구성 확정
  - 후보: 신규 `thumbnailMedia`
  - 기존: `thumbnailPath` 유지
- [x] SEO 탭 구성 확정
  - 후보: `meta.title`, `meta.description`, `meta.image`
- [x] 발행/설정 필드 위치 확정
  - `displayStatus`는 `상태` 라벨로 우측 보조 영역에 배치
  - `viewCount`는 관리자 화면에서 숨기고 기존 데이터 보존
- [x] 레거시 필드 위치 확정
  - 후보: 레거시 탭 또는 접힘 영역
- [x] versions 적용 여부 확정
  - 후보: `maxPerDoc: 15`
  - draft는 1차 보류 권장
- [x] slug 생성 방식 확정
  - 후보: `news-{날짜}-{짧은 suffix}` 또는 수동 slug
- [x] 프론트 fallback 구현
  - [x] 본문: `body` -> `bodyHtml`
  - [x] 이미지: `thumbnailMedia` -> `thumbnailPath`
  - [x] SEO 제목: `meta.title` -> `title`
  - [x] SEO 설명: `meta.description` -> `excerpt`
  - [x] SEO 이미지: `meta.image` -> `thumbnailMedia` -> `thumbnailPath`

## 2차 구현 대상: ArtistPress

> `news`에서 검증한 관리자 UX, Lexical 본문, media/SEO, 프론트 fallback 패턴을 `artist-press`에 최소 변경으로 적용한다.

- [x] `artist-press` 컬렉션 현재 필드/권한/훅 구조 확인
- [x] `title`을 탭 밖 최상단 필드로 이동
- [x] 콘텐츠 탭 구성 적용
  - 신규: `body` Lexical 본문
  - 기존: `actorName`, `generation`, `bodyHtml` 유지
  - 기존 `bodyHtml`은 `body`가 비어 있을 때 Lexical 값으로 자동 변환
- [x] 미디어 탭 구성 적용
  - 신규: `thumbnailMedia`, `agencyLogoMedia`
  - 기존: `thumbnailPath`, `agencyLogoPath` 유지
- [x] SEO 탭 구성 적용
  - 신규: `meta.title`, `meta.description`, `meta.image`
- [x] 발행/설정 필드 위치 적용
  - `displayStatus`는 `상태` 라벨로 우측 보조 영역에 배치
- [x] 레거시 필드 위치 적용
  - `sourceDb`, `sourceTable`, `sourceId`, `bodyHtml`, `thumbnailPath`, `agencyLogoPath`, `legacyMeta`를 레거시/원본 탭에 유지
- [x] versions 적용
  - `maxPerDoc: 15`
- [x] slug 자동 생성 적용
  - 영문/숫자 토큰 우선, 한글만 있으면 `artist-press-{날짜}-{짧은 suffix}`
- [x] 프론트 fallback 구현
  - [x] 목록/상세 라우트: `/artist-press`, `/artist-press/[slug]`
  - [x] 본문: `body` -> `bodyHtml`
  - [x] 이미지: `thumbnailMedia` -> `thumbnailPath`
  - [x] SEO 제목: `meta.title` -> `title`
  - [x] SEO 설명: `meta.description` -> `bodyHtml` 요약 또는 배우명/기수
  - [x] SEO 이미지: `meta.image` -> `thumbnailMedia` -> `thumbnailPath`
- [x] 레거시 썸네일/소속사 로고 media 연결 dry-run 스크립트 추가
  - `npm run payload:legacy:link-artist-press-media`
  - `thumbnailPath`는 `thumbnailMedia`와 `meta.image`에 연결
  - `agencyLogoPath`는 `agencyLogoMedia`에 연결
  - 기본은 dry-run이며 `--write`를 명시해야만 media 생성/연결 수행

## 구현 단계

### Phase 1. 설계 확정

- [ ] 사용자가 컬렉션별 작성 폼을 채운다.
- [ ] 컬렉션별 변경 범위를 표로 정리한다.
- [ ] 대표 컬렉션 1개를 확정한다.
- [ ] 레거시 fallback 정책을 확정한다.
- [ ] 컬렉션별 최소 원본 추적 필드를 확정한다.
  - 기본 후보: `sourceDb`, `sourceTable`, `sourceId`
  - 필요할 때만 `legacyUrl`, `legacyPath`, `legacyMeta`를 추가한다.
  - `wr_*`, `bf_*`, 원본 본문/첨부 컬럼은 운영 필드로 정제하고 최종 Payload 필드로 그대로 복제하지 않는다.
- [ ] migration 위험 항목을 사전에 정리한다.

### Phase 2. 대표 컬렉션 적용

- [x] 대표 컬렉션 파일을 읽고 현재 필드/권한/훅 구조를 확인한다.
- [x] 타이틀 필드를 탭 밖 최상단에 둔다.
- [x] 탭 구조를 적용한다.
- [x] 신규 Lexical 필드를 추가한다.
- [x] 신규 media 관계 필드를 추가한다.
- [x] SEO 필드를 추가한다.
- [x] 필요 시 versions를 제한 개수로 추가한다.
- [x] slug 필드와 자동 생성 로직을 추가한다.
- [x] 기존 레거시 필드를 레거시 영역으로 이동한다.

### Phase 3. 프론트 fallback 적용

- [x] 대표 컬렉션의 목록/상세 렌더링 위치를 확인한다.
- [x] 신규 본문 필드 우선 렌더링을 추가한다.
- [x] 기존 HTML 본문 fallback을 유지한다.
- [x] 신규 media 이미지 우선 렌더링을 추가한다.
- [x] 기존 이미지 경로 fallback을 유지한다.
- [x] SEO metadata fallback을 추가한다.

### Phase 4. 검증

- [x] `npm run payload:generate-types`
- [x] `npm run payload:generate-importmap`
- [x] `npm run typecheck`
- [x] `npm run lint`
- [x] `npm run build`
- [ ] 관리자 화면에서 대표 컬렉션 작성 UX 수동 확인
- [ ] 기존 레거시 데이터 표시 확인
- [ ] 신규 작성 데이터 표시 확인

### Phase 5. Migration

- [ ] `npm run db:local:migrate:create` 실행 전 현재 schema diff를 확인한다.
- [ ] Payload가 묻는 rename/create 질문을 검토한다.
- [ ] 기존 데이터 손실 위험이 있는 선택은 하지 않는다.
- [ ] 대표 컬렉션 변경분만 포함되도록 migration을 확인한다.
- [ ] migration 파일 생성 후 SQL 내용을 검토한다.
- [ ] 로컬 DB에서 migration 적용을 검증한다.

### Phase 6. 확장

- [ ] 대표 컬렉션 패턴을 기준으로 2번째 컬렉션을 선정한다.
- [ ] 같은 방식으로 필드/프론트/migration을 반복한다.
- [ ] 컬렉션별 공통 helper가 실제 중복을 줄일 때만 추출한다.
- [ ] 불필요해진 템플릿 `pages/posts` 모델 정리 여부를 다시 결정한다.

## Migration 주의 항목

현재 `npm run db:local:migrate:create -- --name website-template` 실행 시 기존 스키마 차이에 대한 질문이 먼저 나왔다.

예:

- `artist_press.author_name` 컬럼을 새로 만들 것인지
- 기존 `is_public` 컬럼을 `author_name`으로 rename할 것인지

이 질문은 자동으로 답하면 위험하다. 잘못 선택하면 기존 데이터 구조를 의도와 다르게 바꾸는 migration이 만들어질 수 있다.

따라서 다음 migration 생성 전에는 아래를 먼저 확인한다.

- [ ] 현재 DB schema와 최신 migration 파일의 차이
- [ ] `artist_press` 관련 기존 변경 이력
- [ ] rename으로 판단할 수 있는 명확한 근거가 있는지
- [ ] create column으로 처리해도 기존 데이터 손실이 없는지
- [ ] 신규 Payload Admin UX 변경분과 기존 schema diff를 분리할 수 있는지

## 보류

- [ ] 모든 컬렉션에 versions 일괄 적용
- [ ] 모든 본문 HTML을 Lexical로 즉시 변환
- [ ] 기존 이미지 path 필드 제거
- [ ] Payload template seed 활성화
- [ ] draft/schedule publish 전면 도입

## 이미지 스토리지 이관 메모

- [x] 뉴스 본문 legacy 이미지 URL 스캔 결과 생성
  - `tmp/legacy-assets/news-legacy-url-scan.json`
  - 6,255개 문서, 고유 legacy 이미지 URL 5,807개, URL 출현 5,940건
- [x] 뉴스 이미지 storage-neutral 이관 계획 생성
  - `npm run payload:legacy:plan-news-images`
  - 출력: `tmp/legacy-assets/news-storage-plan.json`
  - 업로드 서비스는 Cloudflare R2로 확정했다. URL별 로컬 후보와 검토 상태는 R2 업로드 전 staging 자료로 유지한다.
  - `/web/data/editor/...` 이미지는 썸네일과 같은 경우와 다른 경우가 섞여 있으므로 자동 치환하지 않고 `needs-review`로 분류한다.
  - 각 `needs-review` 항목에는 같은 뉴스의 썸네일/file-0/file-1 후보 로컬 경로를 포함한다.
- [x] 뉴스 레거시 썸네일을 Payload media 필드에 연결하는 dry-run 리포트 생성
  - `npm run payload:legacy:link-news-thumbnails`
  - 출력: `tmp/legacy-assets/news-thumbnail-media-link-report.json`
  - `thumbnailPath` 기준 6,255건 모두 로컬 legacy 파일로 매칭됨
  - 누락 파일 0건, 미해결 썸네일 경로 0건
  - 실제 쓰기는 `--write` 옵션으로 분리한다. 쓰기 시 같은 media id를 `thumbnailMedia`와 `meta.image`에 연결한다.
- [x] 출신 아티스트 레거시 썸네일/소속사 로고를 Payload media 필드에 연결하는 dry-run 리포트 생성
  - `npm run payload:legacy:link-artist-press-media -- --limit 5`
  - 출력: `tmp/legacy-assets/artist-press-media-link-report.json`
  - 대표 5건 기준 10개 asset 중 dry-run 9건, 레거시 경로 없음 1건, 누락 파일 0건, 미해결 경로 0건
  - 실제 쓰기는 `--write` 옵션으로 분리한다. 쓰기 시 썸네일은 `thumbnailMedia`와 `meta.image`, 소속사 로고는 `agencyLogoMedia`에 연결한다.
- [x] 대학 로고 레거시 이미지를 Payload media 필드에 연결
  - `npm run payload:legacy:link-exam-school-logo-media -- --write`
  - 출력: `tmp/legacy-assets/exam-school-logo-media-link-report.json`
  - 다운로드 리포트 기준 50건 모두 로컬 legacy 파일로 매칭해 media 생성 후 `logoMedia`에 연결
  - 재실행 dry-run 기준 `skippedExisting` 50건, 누락 파일 0건, 미해결 경로 0건
- [x] 운영 이미지 저장소를 Cloudflare R2로 확정
  - 기존 Blob/R2 보류 표현은 R2 기준으로 정리한다.
  - DB/Payload에는 가능한 한 계정 종속 전체 URL보다 `objectKey` 중심으로 저장하고, 공개 URL은 `R2_PUBLIC_BASE_URL`로 조합한다.
  - 세부 이전/임시 계정 기준은 `docs/01-프로젝트-운영-정책.md`를 따른다.
- [x] 개발자 계정 R2 테스트 후 클라이언트 계정 이전을 URL 정책에 반영
  - 개발 중에는 개발자 계정 R2에 업로드해 검증한다.
  - 운영 전환 시 같은 `objectKey`를 유지해 클라이언트 R2로 복사한다.
  - DB/Payload/본문에는 개발자 R2 전체 URL을 저장하지 않고, 환경변수 교체만으로 공개 URL을 바꿀 수 있게 한다.
- [ ] 한글 slug 전면 허용

## 검증 기록

- 2026-04-28
  - `docs`와 `plan`의 활성 문서를 7개 기준 문서로 통합
  - 세부/과거 문서는 `docs/archive/*`, `plan/archive/*`로 이동해 보존
  - `plan/todo.md`를 `plan/현재작업.md`로 변경
  - 현재 DB 이전 기준을 `data/legacy_dumps -> 로컬 MariaDB -> 로컬 Postgres/Payload DB 정제 -> Vercel Neon`으로 문서화
  - 과거 P0/P1/P2/C0 기준과 정적 SQL seed 계열 방향은 현재 판단 기준에서 제외
  - 문서 변경만 수행

- 2026-04-28
  - 개발자 계정 R2에서 먼저 개발 테스트하고 이후 클라이언트 R2로 이전하는 운영 전제를 문서화
  - `objectKey`를 정본으로 저장하고 `R2_PUBLIC_BASE_URL`로 공개 URL을 조합하는 정책 강화
  - 개발자 계정 `r2.dev` URL, 임시 커스텀 도메인 전체 URL, 완성 public URL을 DB/Payload에 저장하지 않는 금지 규칙 추가
  - 문서 변경만 수행

- 2026-04-28
  - 데이터 저장소를 Cloudflare R2로 확정
  - `docs/01-프로젝트-운영-정책.md`, `docs/02-레거시-마이그레이션-정책.md`, `docs/03-Payload-admin-운영-UX.md`의 Blob/R2 보류 표현을 R2 기준으로 업데이트
  - 문서 변경만 수행

- 2026-04-28
  - 대학 로고 설정 관리자 UX 정리: 후기 수 필드/컬럼 제거, 원본 파일명, 로고 파일, 가로, 세로 필드를 등록 화면/목록에서 숨김
  - 학교 슬러그는 영문 소문자, 숫자, 하이픈만 허용하고 안내 문구 추가
  - 로고 이미지는 Pages/Posts와 같은 `media` 업로드 관계 필드 `logoMedia`로 등록하도록 변경
  - `npm run payload:legacy:link-exam-school-logo-media -- --write`: 50건 media 생성 및 `logoMedia` 연결
  - `npm run payload:legacy:link-exam-school-logo-media`: 재검증 기준 `skippedExisting` 50건, 미해결 0건
  - `npm run payload:generate-types`: 통과
  - `npm run payload:generate-importmap`: 통과
  - `npm run typecheck`: 통과
  - `npm run lint`: 통과
  - `npm run build`: 통과
  - `npm run build` 중 pg SSL mode 경고가 출력되었으나 빌드는 성공

- 2026-04-28
  - `artist-press` 관리자 UX 1차 적용: title 최상단, 콘텐츠/미디어/SEO/레거시 탭, sidebar 상태, Lexical `body`, `thumbnailMedia`, `agencyLogoMedia`, versions 15개
  - `artist-press.body`가 비어 있으면 `bodyHtml`을 Payload 공식 `convertHTMLToLexical` + `jsdom`으로 자동 변환
  - `artist-press` 프론트 목록/상세 라우트 추가: `/artist-press`, `/artist-press/[slug]`
  - 신규 필드 우선 fallback 적용: `body` -> `bodyHtml`, `thumbnailMedia` -> `thumbnailPath`, `meta.image` -> `thumbnailMedia` -> `thumbnailPath`
  - `npm run payload:legacy:link-artist-press-media -- --limit 5`: 10개 asset 중 dry-run 9건, 레거시 경로 없음 1건, 미해결 0건
  - `npm run payload:generate-types`: 통과
  - `npm run payload:generate-importmap`: 통과
  - `npm run typecheck`: 통과
  - `npm run lint`: 통과
  - `npm run build`: 통과
  - `npm run build` 중 pg SSL mode 경고가 출력되었으나 빌드는 성공
  - `curl -I http://localhost:3000/artist-press`: HTTP 200
  - `curl -I http://localhost:3000/artist-press/artist-press-10`: HTTP 200

- 2026-04-28
  - `news` 프론트 목록/상세 라우트 추가: `/news`, `/news/[slug]`
  - 신규 필드 우선 fallback 적용: `body` -> `bodyHtml`, `thumbnailMedia` -> `thumbnailPath`, `meta.image` -> `thumbnailMedia` -> `thumbnailPath`
  - `thumbnailPath` fallback은 `legacyMeta.attachments[].localPath`를 우선 사용해 로컬 `public/legacy` 파일로 연결
  - `curl -I http://localhost:3000/news`: HTTP 200
  - `curl -I http://localhost:3000/news/news-bnbhighteen-1891`: HTTP 200
  - `npm run typecheck`: 통과
  - `npm run lint`: 통과
  - `npm run build`: 통과
  - `npm run build` 중 pg SSL mode 경고가 출력되었으나 빌드는 성공

- 2026-04-28
  - `news` 관리자 UX 1차 적용: 저장 버튼 강조, title 최상단, 콘텐츠/미디어/SEO/발행/레거시 탭, sidebar slug, Lexical `body`, `thumbnailMedia`, versions 15개
  - `news` 발행/설정 탭 제거, `노출 상태`를 `상태`로 변경해 우측 보조 영역으로 이동, 조회수 항목 숨김
  - `news` Lexical 툴바 확장: 기본 추천 기능, heading, blockquote, horizontal rule, fixed/inline toolbar
  - `news.body`가 비어 있으면 `bodyHtml`을 Payload 공식 `convertHTMLToLexical` + `jsdom`으로 자동 변환
  - `npm run payload:generate-types`: 통과
  - `npm run payload:generate-importmap`: 통과
  - `npm run typecheck`: 통과
  - `npm run lint`: 통과
  - `npm run build`: 통과
  - `npm run build` 중 pg SSL mode 경고가 출력되었으나 빌드는 성공
  - `curl -I http://localhost:3000`: HTTP 200
  - `curl -I http://localhost:3000/admin`: HTTP 200

- 2026-04-28
  - `npm run payload:generate-types`: 통과
  - `npm run payload:generate-importmap`: 통과
  - `npm run typecheck`: 통과
  - `npm run lint`: 통과
  - `npm run build`: 통과
  - `curl -I http://localhost:3000`: HTTP 200
  - `curl -I http://localhost:3000/admin`: HTTP 200
