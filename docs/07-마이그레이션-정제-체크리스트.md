# 마이그레이션 정제 체크리스트

> 목적: 로컬 Postgres/Payload DB에서 정제가 끝난 테이블을 확인하고, 원격 Vercel Neon DB 이전 전에 누락을 막는다.
> 마지막 갱신: 2026-06-25

## 현재 순서 판단

사용자가 정리한 순서는 큰 방향이 맞다.

```text
1. 원격/원본 dump 확보
2. 로컬 MariaDB에 dump 복원
3. 로컬 MariaDB work table을 로컬 Postgres/Payload DB로 적재
4. 로컬 Postgres/Payload DB 정제 및 검증
5. 원격 Vercel Neon DB로 schema/data 이전
6. 완료
```

주의할 점은 3번이다. 현재 저장소 기준으로는 MariaDB 원본 DB를 PostgreSQL로 그대로 변환하는 단계가 아니라, `bnb_legacy_work.*` work table을 Payload 컬렉션 구조에 맞게 정제 적재하는 단계로 본다.

5번은 4번의 로컬 검증이 끝난 뒤에만 진행한다. 원격 Neon에는 검증된 migration과 데이터만 반영한다.

## 전체 DB 백업 기록

### 2026-06-25 Neon 최초 이전 기록

- 대상: 로컬 Docker Postgres `bnb_renewal`에서 Neon `neondb`로 schema/data 최초 이전
- 로컬 사전 백업: `/Users/arisnoba/Backups/bnb-renewal/postgres/bnb_renewal-20260625-150119-pre-neon-first-migration.dump`
- 로컬 이전 소스 dump: `/Users/arisnoba/Backups/bnb-renewal/postgres/bnb_renewal-20260625-150155-local-source-for-neon.dump`
- Neon 사전 백업: `/Users/arisnoba/Backups/bnb-renewal/postgres/neon-neondb-20260625-150227-pre-first-restore.dump`
- 진행 순서:
  - 로컬 Payload migration 누락분 `20260624_120000_social_links_sns_type`, `20260624_130000_media_uploads_prefix` 적용
  - `payload:generate-types` 실행
  - 로컬 source dump 생성 및 restore list/checksum 검증
  - Neon 기존 `public` schema 사전 백업 후 초기 schema를 reset하고 source dump restore
- 복원 결과:
  - 주요 카운트 일치 확인: `users` 7, `media` 24,922, `teachers` 85, `profiles` 1,469, `news` 6,255, `artist_press` 804, `screen_appearances` 1,233, `direct_castings` 268, `agencies` 78
  - `payload_migrations` 130건, 최신 migration `20260624_130000_media_uploads_prefix`
  - `media.prefix` 기본값 `media/uploads`
- 검증:
  - 복원 후 `npm run typecheck` 통과
  - 대표 R2 공개 URL 샘플 9개 `HEAD 200` 확인
- 주의:
  - 첫 `pg_restore --clean` 시도는 기존 원격 schema의 stale dependency 때문에 실패했지만 `--single-transaction`으로 rollback됐다.
  - 이후 사전 백업이 있는 상태에서 Neon `public` schema를 reset하고 clean restore를 완료했다.
  - `npm run db:remote:migrate`는 Payload dev-mode data loss prompt가 떠서 중단했다. 원격 `payload_migrations`가 로컬과 동일해 추가 적용은 하지 않았다.
  - Neon 접속 시 `sslmode` 경고가 반복된다. 현재 동작은 통과하지만 이후 connection string을 명시적으로 `sslmode=verify-full`로 정리하는 것이 좋다.

### 2026-06-25 Neon R2 path 참조 보정

- 대상: Neon `neondb`
- 사전 백업: `/Users/arisnoba/Backups/bnb-renewal/postgres/neon-neondb-20260625-152645-pre-r2-path-rewrite.dump`
- 목적: R2 객체는 업로드되어 있으나 운영 DB가 `/legacy/...` 문자열을 참조하던 대표 이미지 필드를 `/media/...` R2 object path로 보정
- 업데이트 결과:
  - `casting_appearances.thumbnail_path`: 372건 업데이트
  - `exam_passed_reviews.student_image_path`: 165건 업데이트
  - `exam_results.thumbnail_path`: 177건 업데이트
- 검증:
  - 위 3개 필드의 `/legacy/...` 잔여 0건
  - 업데이트된 `/media/...` path의 R2 object 누락 0건
  - 공개 URL 샘플 9건 `HEAD 200` 확인
- 비고:
  - `casting_appearances.thumbnail_path` 1건은 기존 관리자 업로드로 보이는 R2 전체 URL을 이미 참조하고 있어 이번 `/legacy/...` 보정 대상에서 제외했다.

### 2026-05-27 로컬 Postgres 전체 백업

- 대상: Docker Compose `postgres` 서비스의 로컬 Postgres DB `bnb_renewal` 전체
- 범위: 특정 테이블이 아니라 DB 전체 schema/data/blob을 `pg_dump -Fc -b` 커스텀 포맷으로 백업
- 백업 파일: `tmp/db-backups/bnb_renewal_local_20260527_170202.dump`
- 체크섬 파일: `tmp/db-backups/bnb_renewal_local_20260527_170202.dump.sha256`
- SHA-256: `d9196d4a4ba3a35ace48a30a037802bc93e77abb9a6479508f7c7d7094225e43`
- 파일 크기: `6.1M`
- 검증 명령: `docker compose exec -T postgres pg_restore -l < tmp/db-backups/bnb_renewal_local_20260527_170202.dump`
- 검증 결과: restore list 항목 1,257개 확인
- 백업 시점 FAQ 확인: `faqs` 33건, `faqs_centers` 78건, 센터 없는 FAQ 0건
- 주의: 같은 폴더의 `bnb_renewal_local_20260527_170143.dump`는 첫 시도에서 생성된 0B 무효 파일이다. 실제 백업으로 사용하지 않는다.

복원은 현재 DB를 덮어쓰는 작업이므로 실행 전에 반드시 대상 DB를 재확인한다.

```bash
docker compose exec -T postgres pg_restore --clean --if-exists -U postgres -d bnb_renewal < tmp/db-backups/bnb_renewal_local_20260527_170202.dump
```

## 4번 정제 완료 기준

각 테이블은 아래 항목을 모두 확인한 뒤에만 `정제 완료`로 체크한다.

- 로컬 Postgres/Payload 적재가 완료됐다.
- 정제 전 원본 추적 값(`sourceDb`, `sourceTable`, `sourceId` 또는 동등한 값)을 확인했다.
- 정제 완료 후 원본 추적 값을 제거했다면 migration과 backfill/검증 리포트로 재현 경로가 남아 있다.
- 필수 운영 필드가 비어 있지 않다.
- 센터 매핑이 맞다.
- 공개/비공개 상태 값이 맞다.
- 레거시 게시 날짜가 있는 컬렉션은 `published_at`과 Payload `created_at`이 정렬 기준에 맞게 일치한다.
- 대표 이미지, 로고, 본문 이미지, 첨부 이미지 경로 또는 media relation이 확인됐다.
- `/test/postgres/*` 검수 화면에서 샘플이 정상 표시된다.
- MariaDB work table 카운트와 Postgres/Payload 카운트 차이를 설명할 수 있다.
- 정제 과정에서 의도적으로 제외한 레코드는 사유가 기록됐다.

## 테이블별 체크리스트

- [x] `teachers`
  - Postgres: `teachers`
  - MariaDB work table: `bnb_legacy_work.teachers`
  - 검수 URL: `/test/postgres/teachers`
  - 주요 확인: 이름, 센터, 상태, 정렬, 대표 이미지, 강사 파일 연결, 버전 기능
  - 정제 결과: 로컬 Postgres `teachers`에서 `source_db/source_table/source_id/legacy_meta/profile_image_path` 제거, `profileImageMedia` 필수화
  - 검증 결과: 총 86건, 공개 86건, 초안 0건, 이름/슬러그/대표 이미지 86건, photo1 85건
  - 관계/배열 결과: 센터 매핑 85명/108행, 필모그래피 85명/293행, 대표작 45명/286행
  - 이미지 결과: 레거시 프로필 경로는 `profileImageMedia`로 전환했고 `photoImage1~6`는 `/legacy/teachers/...` 경로로 정규화했다.
  - R2 결과: 프로필 이미지 `media/teachers/profile-images/{teacher_id}` 86개 업로드, Payload media 86건 생성 및 `profileImageMedia` relation 연결
  - 적용 migration: `20260430_193000_teacher_cleanup_versions`, `20260526_130000_teacher_profile_image_media`, `20260526_212000_teacher_profile_image_media_required`
- [x] `curriculums`
  - Postgres: `curriculums`
  - MariaDB work table: `bnb_legacy_work.teacher_lessons`
  - 검수 URL: `/test/postgres/curriculums`
  - 주요 확인: 강사 매핑, 과목, 카테고리, 주차/레슨 구조
  - 정제 결과: 로컬 Postgres `curriculums`에서 원본 추적/임시 파싱 컬럼 제거
  - 검증 결과: 총 125건, 제목/분반/정원 125건, 강사 매핑 124건
  - 관계/배열 결과: 커리큘럼 레슨 1,000행
  - 적용 migration: `20260429_210000_curriculum_policy_restructure`, `20260429_213000_drop_curriculum_legacy_fields`
- [x] `highteen-special-classes`
  - Postgres: `highteen_special_classes`
  - MariaDB work table: `bnb_legacy_work.highteen_special_classes`
  - 원본 테이블: `bnbhighteen.g5_write_new_specialclass`
  - 검수 URL: `/test/postgres/highteen-special-classes`, `/test/mariadb/highteen-special-classes`
  - 주요 확인: 하이틴센터 전용 특강 메뉴, 제목, 본문, 유튜브 URL, 대표 이미지, 첨부 이미지, 공개 상태
  - 적재 결과: 별도 Payload 컬렉션 `highteen-special-classes`를 `교육` 그룹 하위 메뉴로 추가하고, 관리자 메뉴는 하이틴센터 담당자와 전체 관리자에게만 노출
  - 검증 결과: 원본/work table 14건, 로컬 Postgres 14건, 본문 14건, 유튜브 URL 14건, 대표 이미지 경로 14건, 발행일 14건
  - 관계/배열 결과: 센터 매핑 `highteen` 14행, 첨부 이미지 배열 28행/14건
  - 정리 결과: 요청에 따라 Payload 필드와 로컬 Postgres 운영 테이블에서 `조회수/view_count` 제거
  - 이미지 결과: `/legacy/highteen-special-classes/...` 이미지 42개 다운로드, Payload media 28건 생성, 대표 이미지 14건과 첨부 이미지 28건을 media relation으로 연결
  - R2 결과: 이미지 `media/highteen-special-classes/images/{highteen_special_class_id}` 168개 업로드
  - 정제 결과: 로컬 Postgres `highteen_special_classes`에서 `thumbnail_path/source_db/source_table/source_id/legacy_meta` 제거, `highteen_special_classes_gallery_images`에서 `image_path` 제거
  - 적용 migration: `20260518_140000_highteen_special_classes`, `20260518_143000_drop_highteen_special_class_view_count`, `20260520_121000_highteen_special_class_media_fields`, `20260520_122000_drop_highteen_special_class_legacy_fields`
- [x] `agencies`
  - Postgres: `agencies`
  - MariaDB work table: `bnb_legacy_work.agencies`
  - 검수 URL: `/test/postgres/agencies`
  - 주요 확인: 배우 목록, 표시 순서, 프로필 이미지, 본문
  - 정제 결과: 로컬 Postgres `agencies`에서 `body_html/profile_image_path/source_db/source_table/source_id/slug/legacy_meta` 제거
  - 검증 결과: 총 78건, 제목 78건, 로고 media relation 78건
  - 관계/배열 결과: 배우 목록 114행
  - 적용 migration: `20260430_120000_drop_agency_body_html`, `20260430_121000_drop_agency_actor_profile_image_path`, `20260430_122000_agency_logo_media`, `20260430_123000_drop_agency_profile_image_path`, `20260430_124000_drop_agency_legacy_fields`
- [x] `artist-press`
  - Postgres: `artist_press`
  - MariaDB work table: `bnb_legacy_work.artist_press`
  - 검수 URL: `/test/postgres/artist-press`
  - 주요 확인: 배우명, 기수, 본문, 썸네일, 소속사 로고, SEO/media relation
  - 정제 결과: 로컬 Postgres `artist_press`에서 `source_db/source_table/source_id/body_html/thumbnail_path/agency_logo_path/legacy_meta` 제거, 버전 테이블의 동일 레거시 컬럼 제거
  - 검증 결과: 총 804건, 제목/배우명/기수/본문 804건, 썸네일 media relation 750건, 소속사 로고 media relation 802건, SEO 이미지 750건
  - 본문 이미지 결과: Lexical upload node 844개, media 참조 844/844건
  - R2 결과: 썸네일 `media/artist-press/thumbnails/{artist_press_id}` 1,503개 업로드, 소속사 로고 `media/artist-press/agency-logos/{artist_press_id}` 823개 업로드, 본문 이미지 `media/artist-press/body-images/{artist_press_id}` 840개 원본 업로드
  - 적용 migration: `20260511_200000_drop_artist_press_legacy_fields`
- [x] `profiles`
  - Postgres: `profiles`
  - MariaDB work table: `bnb_legacy_work.profiles`
  - 검수 URL: `/test/postgres/profiles`
  - 주요 확인: 이름, 센터, 필터, 프로필 이미지, 갤러리 이미지, 이력 파싱, 버전 기능
  - 정제 결과: 로컬 Postgres `profiles`에서 `source_db/source_table/source_id/legacy_meta` 제거
  - 백필 결과: 레거시 첨부 5,494개 확인, 갤러리 1번 이미지 1,461건, 누락 파일 0건
  - R2 결과: 프로필 이미지 `media/profiles/profile-images/{profile_id}` 1,465개 원본 업로드, Payload media 1,465건 생성 및 `profileImageMedia` relation 연결, invalid media refs 0건
  - 적용 migration: `20260430_190000_drop_profile_legacy_fields`, `20260430_191000_profile_versions`, `20260430_192000_profile_gallery_images`, `20260526_150000_profile_image_media`
- [x] `audition-schedules`
  - Postgres: `audition_schedules`
  - MariaDB work table: `bnb_legacy_work.audition_schedules`
  - 검수 URL: `/test/postgres/audition-schedules`
  - 주요 확인: 일정 시작/종료일, 일정 유형 select, 공개 상태
  - 정리 메모: 관리자 상세 화면의 `일정` 탭 제거, 일정 유형은 `촬영/shooting`, `일정/schedule`, `오디션/audition` select로 고정
  - 정제 결과: `중복 기준 키` 관리자 UI 제거, 로컬 Postgres `audition_schedules`에서 `source_db/source_table/source_id/slug/dedupe_key/schedule_start_raw/schedule_end_raw/legacy_meta` 제거
  - 날짜 입력 정책: Payload 기본 date 필드는 단일 필드용이며 range 저장 필드는 별도 커스텀이 필요하다. 로컬 데이터는 3,781건 중 3,634건이 1일 일정이라 종료일 미입력 시 시작일로 저장 보정한다.
  - 적용 migration: `20260511_180000_audition_schedule_event_type_select`, `20260511_181000_drop_audition_schedule_legacy_fields`
- [x] `screen-appearances`
  - Postgres: `screen_appearances`
  - MariaDB work table: `bnb_legacy_work.screen_appearances`
  - 검수 URL: `/test/postgres/screen-appearances`
  - 주요 확인: 센터, 출연자 입력 방식, 연결된 프로필 또는 직접 입력, 출연 유형, 방영일 표시, 프로필 이미지, 썸네일, 본문 이미지
  - 정제 결과: 로컬 Postgres `screen_appearances`에서 `body/source_db/source_table/source_id/slug/legacy_meta` 제거
  - 구조화 결과: 기존 본문에서 소개 문장, 필모그래피, 본문 이미지를 분리했고 본문 richText 대신 구조화 필드와 미디어 배열로 관리
  - R2 결과: 본문 이미지 `media/screen-appearances/body-images/{screen_appearance_id}` 5,997개 원본 업로드, Payload size filename 0건
  - 프로필 연결 결과: 총 1,230건, 프로필 연결 996건, 직접 입력/미연결 234건, 연결 프로필 566명
  - 날짜 결과: 방영일 누락 0건, 누락값은 발행일 기준으로 backfill
  - 검수 리포트: `docs/reports/screen-appearances-unlinked-profiles.md`
  - 적용 migration: `20260512_140000_screen_appearance_local_image_paths`, `20260512_150000_screen_appearance_field_types`, `20260512_160000_screen_appearance_structured_body`, `20260512_170000_screen_appearance_profile_links`, `20260512_171000_screen_appearance_profile_links_backfill`, `20260512_180000_screen_appearance_actor_input_mode`, `20260512_190000_drop_screen_appearance_body_legacy_fields`, `20260512_191000_backfill_screen_appearance_air_dates`
- [x] `casting-appearances`
  - Postgres: `casting_appearances`
  - MariaDB work table: `bnb_legacy_work.casting_appearances`
  - 검수 URL: `/test/postgres/casting-appearances`
  - 주요 확인: 캐스팅 상태, 제작사/방송사, 출연진, 공개 상태
  - 정제 결과: 로컬 Postgres `casting_appearances`에서 `source_db/source_table/source_id/slug/body_html/legacy_meta` 제거
  - 검증 결과: 총 414건, 제목/캐스팅 상태/발행일 414건, 캐스팅 회사 353건, 썸네일 373건
  - 관계/배열 결과: 센터 매핑 아트 153건/하이틴 129건/키즈 132건, 출연자 배열 41,792행/395건
  - 이미지 결과: `thumbnail_path` 373건을 `/legacy/casting-appearances/...` 경로로 정규화했고 로컬 파일 누락 0건
  - R2 결과: 썸네일 `media/casting-appearances/images/{work_id}` 2,984개 업로드
  - 적용 migration: `20260513_171500_casting_appearance_legacy_cleanup`
- [x] `direct-castings`
  - Postgres: `direct_castings`
  - MariaDB work table: `bnb_legacy_work.direct_castings`
  - 원본 테이블: `baewoo/bnbhighteen/kidscenter.g5_write_new_direct2`, `bnbhighteen/kidscenter.g5_write_new_direct_img`, `bnbhighteen/kidscenter.g5_write_new_direct_enm`, `baewoo/bnbhighteen/kidscenter.g5_write_new_direct_bx`
  - 검수 URL: `/test/postgres/direct-castings`, `/test/mariadb/direct-castings`
  - 주요 확인: 캐스팅 회사명, 노출 센터 배열, 작품/항목명, 본문 이미지 media relation, 대표 이미지 media relation, 공개 상태
  - 적재 결과: 별도 Payload 컬렉션 `direct-castings`를 `캐스팅/오디션` 그룹 하위 메뉴 `다이렉트캐스팅`으로 추가하고, 관리자 메뉴와 접근권한은 입시센터만 제외
  - 중복 정리: 레거시 원본 687행을 회사명+작품명 기준 work table 307건으로 통합하고, 중복 센터는 `centers` 배열 655링크로 보존
  - 검증 결과: 로컬 Postgres 307건, `centers` 링크 655건, `body_html` 307건, 대표 이미지 media relation 307건, 대표 media 참조 누락 0건
  - 정제 결과: 로컬 Postgres `direct_castings`에서 `source_center/thumbnail_path/body_html/source_db/source_table/source_id/legacy_meta` 제거
  - 회사/센터 결과: 유캐스팅 59건, IMGround 38건, BNB캐스팅 86건, BX모델에이전시 124건 / 센터 링크 아트 170건, 하이틴 232건, 키즈 253건
  - 관리자 정리: `연도별 이력` 탭과 `workItems` 배열은 제거하고, 본문 이미지가 포함된 본문 탭으로 관리
  - 이미지 결과: 다운로드 대상 1,067개(대표 307개, 본문 760개), 신규 다운로드 796개, 기존 파일 재사용 271개, 실패 0건
  - media 결과: 본문 upload media 참조 760건/307문서, 본문 media 참조 누락 0건
  - R2 결과: 대표 이미지 `media/direct-castings/thumbnails` 444개 업로드, 본문 이미지 `media/direct-castings/body-images` 698개 업로드
  - 본문 R2 기준: 현재 Lexical `body` upload node가 참조하는 media만 업로드하고, 본문용 Payload size filename은 모두 `NULL` 처리
  - 적용 migration: `20260518_160000_direct_castings`, `20260518_170000_direct_casting_thumbnail_media`, `20260518_180000_direct_casting_centers_and_body_html`, `20260518_190000_drop_direct_casting_work_items`, `20260520_120000_drop_direct_casting_legacy_fields`
- [x] `casting-directors`
  - Postgres: `casting_directors`
  - MariaDB work table: `bnb_legacy_work.castings`
  - 검수 URL: `/test/postgres/casting-directors`
  - 주요 확인: 인물명, 회사, 카테고리, 경력 파싱, 프로필 이미지
  - 정제 결과: 로컬 Postgres `casting_directors`에서 `source_db/source_table/source_id/slug/legacy_meta/profile_image_path` 제거
  - 검증 결과: 총 10건, 이름/회사 10건, 프로필 media relation 10건, media 참조 누락 0건
  - 관계/배열 결과: 센터 매핑 29행, 경력 84행
  - 적용 migration: `20260429_120000_casting_director_profile_image_media`, `20260429_130000_casting_director_career_items`, `20260511_160000_drop_casting_director_legacy_fields`
- [x] `exam-passed-reviews`
  - Postgres: `exam_passed_reviews`
  - MariaDB work table: `bnb_legacy_work.exam_passed_reviews`
  - 검수 URL: `/test/postgres/exam-passed-reviews`
  - 주요 확인: 학교 매핑, 학생 이미지, 학교 로고 relation, 합격후기 본문
  - 진행 결과: 합격후기 본문 중 제목/프로필과 중복되는 이름/합격현황을 `studentName`/`resultSummary`로 구조화하고, 질문/답변은 `interviews` 배열로 분리
  - 검증 결과: 총 165건, 학생명 165건, 합격현황 165건, 학교 relation 165건, 학생 이미지 로컬 경로 165건
  - 관계/배열 결과: 센터 매핑 `exam` 165행, 인터뷰 727행/165건
  - 정제 결과: 로컬 Postgres `exam_passed_reviews`에서 `source_db/source_table/source_id/slug/body_html/school_name/school_logo_slug/school_logo_path/legacy_meta/generate_slug/cohort` 제거
  - R2 결과: 학생 이미지 `media/exam-passed-reviews/images/{work_id}` 1,320개 업로드
  - 적용 migration: `20260427_170000_exam_review_school_relation`, `20260511_163000_exam_passed_review_structured_content`, `20260511_170000_drop_exam_passed_review_legacy_fields`, `20260521_140000_drop_exam_passed_review_cohort`
- [x] `exam-passed-videos`
  - Postgres: `exam_passed_videos`
  - MariaDB work table: `bnb_legacy_work.exam_passed_videos`
  - 검수 URL: `/test/postgres/exam-passed-videos`
  - 주요 확인: 유튜브 URL, 썸네일, 제목, 공개 상태
  - R2 결과: 썸네일 `media/exam-passed-videos/thumbnails/{source_id}` 256개 업로드
- [x] `exam-results`
  - Postgres: `exam_results`
  - MariaDB work table: `bnb_legacy_work.exam_results`
  - 검수 URL: `/test/postgres/exam-results`
  - 주요 확인: 결과 유형, 썸네일, 제목, 공개 상태
  - R2 결과: 이미지 `media/exam-results/images/{work_id}` 1,416개 업로드
- [x] `exam-school-logos`
  - Postgres: `exam_school_logos`
  - MariaDB work table: `bnb_legacy_work.exam_school_logos`
  - 검수 URL: `/test/postgres/exam-school-logos`
  - 주요 확인: 학교명, 학교 슬러그, 로고 media relation
- [x] `news`
  - Postgres: `news`
  - MariaDB work table: `bnb_legacy_work.news`
  - 검수 URL: `/test/postgres/news`
  - 주요 확인: 제목, 카테고리, 본문 Lexical fallback, 썸네일/media, SEO fallback
  - 4단계 정제 계획:
    - [x] 1차: 현황 진단 및 자동 전환 가능 범위 확인
      - 완료일: 2026-05-13
      - 로컬 Postgres 기준 총 6,255건
      - 필수 운영 필드: 제목 6,255건, 슬러그 6,255건, 카테고리 6,255건, published_at 6,255건
      - 슬러그 중복 그룹 0건, display_status는 6,255건 모두 `published`
      - 센터 매핑: art 2,908건, kids 2,136건, highteen 677건, exam 534건
      - 카테고리 분포: 15종, 최대 `캐스팅확정` 2,295건
      - 현재 본문 상태: `body` 0건, `body_html` 6,255건
      - 본문 이미지 진단: HTML 이미지 포함 문서 6,207건, 이미지 occurrence 5,941개, unique legacy URL 5,807개, 후보 로컬 파일 누락 0건, 수동 확인 필요 URL 2개
      - 썸네일 media dry-run: 6,254건 자동 매칭 가능, 기존 연결 1건, unresolved 0건
      - 생성 리포트: `tmp/legacy-assets/news-storage-plan.json`, `tmp/legacy-assets/news-thumbnail-media-link-report.json`
    - [x] 2차: 뉴스 보존 정책과 migration 대상 ID 확정
      - 완료일: 2026-05-13
      - 기준: 아직 전체 6,255건을 모두 신규 media로 올릴 요건이 확정되지 않았으므로 실제 이미지 업로드/연결은 보류
      - 검토 축: 센터, 연도, 카테고리, 공개 유지 여부, 중복/저가치 글 제외 여부
      - 확정 정책: 전체 6,255건은 DB에 보존하되, 2020년 이후 5,894건은 `published` 유지, 2019년 이전 또는 보정 날짜 361건은 `draft` 전환
      - media 처리 범위: 3차 media 처리 대상은 `published_at >= 2020-01-01`인 5,894건으로 제한
      - 생성 리포트: `tmp/legacy-assets/news-retention-candidates.csv`, `tmp/legacy-assets/news-media-allowlist-2020-plus.csv`, `tmp/legacy-assets/news-review-pre2020-or-invalid.csv`
      - 정책 문서: `docs/news-migration-retention-policy.md`
      - 적용 migration: `20260513_190000_news_pre2020_draft`
    - [x] 3차: 보존 대상으로 확정된 news만 썸네일을 `thumbnailMedia`/`meta.image` media relation으로 연결하고 검증
      - 완료일: 2026-05-13
      - 기준: 1차 dry-run 기준 전체 썸네일 6,254건 자동 매칭 가능, 로컬 파일 누락 0건
      - 처리 범위: 확정 allowlist `tmp/legacy-assets/news-media-allowlist-2020-plus.csv`에 포함된 news만 대상
      - 적용 결과: 2020년 이후 5,894건 모두 `thumbnail_media_id`/`meta_image_id` 연결
      - 제외 확인: 2019년 이전 또는 보정 날짜 draft 361건 media 연결 0건
      - media 결과: `media/news/thumbnails` prefix 5,893건, 기존 media 참조 1건, 로컬 파일 누락 0건
      - 예외 처리: 깨진 JPEG 9건은 재인코딩 fallback으로 media 생성 후 연결
      - 검증 결과: invalid thumbnail refs 0건, invalid meta image refs 0건, writeError 0건
      - 생성 리포트: `tmp/legacy-assets/news-thumbnail-media-link-report-2020-plus-write.json`
    - [x] 4차: 보존 대상으로 확정된 news의 본문/레거시 fallback 최종 정리
      - 완료일: 2026-05-14
      - 기준: 1차 본문 이미지 occurrence 5,941개 중 수동 확인 필요 2개
      - 본문 처리: 보존 대상으로 확정된 2020년 이후 5,894건 모두 `body_html`을 Lexical `body`로 실제 저장
      - 본문 이미지 처리: Lexical 본문 `pending` upload 노드 6,269개를 `media/news/body-images` media relation으로 연결, pending 0건
      - media 유지: 2020년 이후 5,894건 모두 `thumbnailMedia`/`meta.image` 유지, invalid refs 0건
      - 정제 결과: 로컬 Postgres `news`에서 `source_db/source_table/source_id/body_html/thumbnail_path/legacy_meta` 제거, 버전 테이블의 동일 레거시 컬럼 제거
      - 코드 정리: `News` 컬렉션 레거시 탭과 `bodyHtml` 자동 fallback 훅 제거, public 목록/상세/SEO의 `thumbnailPath`/`legacyMeta`/`bodyHtml` fallback 제거
      - 적용 migration: `20260514_150000_news_body_and_legacy_cleanup`
      - 생성 리포트: `tmp/legacy-assets/news-body-media-backfill-write.json`, `tmp/legacy-assets/news-body-media-backfill-retry-write.json`
      - 검증 결과: 2020년 이후 `body` 5,894/5,894건, 본문 upload refs 6,269건, invalid upload refs 0건, 2019년 이전 draft 361건 `body` 0건, 제거 대상 컬럼 0건
      - R2 메모: 로컬 정제 체크리스트는 완료로 본다. 뉴스 R2 업로드는 공통 자산/R2 게이트의 별도 후속 작업으로 남긴다.
- [x] `star-cards`
  - Postgres: `star_cards`
  - MariaDB work table: `bnb_legacy_work.star_cards`
  - 원본 테이블: `baewoo.g5_write_new_starcard`
  - 검수 URL: `/test/postgres/star-cards`, `/test/mariadb/star-cards`
  - 주요 확인: 스타카드 제휴업체명, 중복 업체 통합, 노출 센터 배열, 본문, 이미지 배열 media relation, 첫 번째 이미지의 대표 이미지 사용, 공개 상태
  - 적재 결과: 별도 Payload 컬렉션 `star-cards`를 `운영/소식` 그룹 하위 메뉴 `스타카드 제휴업체`로 추가
  - 중복 정리: `https://baewoo.me/web/bbs/board.php?bo_table=new_starcard` 목록에 노출된 25건만 유지하고, 나머지 13건은 로컬 Postgres에서 제거
  - 검증 결과: 로컬 MariaDB work table 25건, 로컬 Postgres 27건, 제목 27건, 영문명 기반 슬러그 27건/중복 0건, 이미지 media relation 117/117개, media invalid refs 0건, 공개 26건, 비공개 1건
  - 센터 결과: 27건 모두 `ALL` 센터로 지정
  - 본문 정리: Payload `body`와 기존 `body_html`에서 `네이버지도보기` 문구 제거 확인 0건, 이미지는 `imageMedia` upload relation 배열로 관리하고 첫 번째 이미지를 대표 이미지로 사용
  - 이미지 처리: `/public/star-card`의 25개 업체 폴더에서 105개 이미지를 `media/star-cards/images/{star_card_id}` prefix로 생성/재사용했고, `클레어치과의원`과 `빌리프 성형외과`는 신규 Payload row로 생성
  - 비공개 유지: `글로우 뷰티`는 `archived` 상태로 유지했고, 폴더가 없는 `클로엔 성형외과`는 기존 이미지 relation을 유지한 채 정렬만 보정
  - 정제 결과: 로컬 Postgres `star_cards`에서 `logo_path/source_db/source_table/source_id/body_html/view_count/legacy_meta/logo_media_id` 제거, `star_cards_body_images`에서 `image_path` 제거
  - 적용 migration: `20260518_210000_star_cards`, `20260518_211000_star_card_body_images`, `20260518_212000_drop_star_card_summary_and_body_image_order`, `20260518_213000_drop_star_card_body_image_source_file`, `20260520_124000_star_card_media_fields`, `20260520_125000_drop_star_card_legacy_fields`, `20260526_210000_drop_star_card_logo_media`
  - 생성 리포트: `tmp/legacy-assets/star-cards-image-download-report.json`, `tmp/legacy-assets/star-card-media-link-report.json`, `tmp/legacy-assets/star-card-local-folder-sync-report.json`

## 공통 자산 체크

- [x] `media` relation: Payload 문서가 로컬 파일 경로 문자열만 보지 않고 필요한 곳에서 `media` relation을 참조한다.
  - 2026-05-27 로컬 Postgres 확인: `agencies.logo_media_id`, `artist_press.thumbnail_media_id/agency_logo_media_id`, `casting_directors.profile_image_media_id`, `direct_castings.thumbnail_media_id`, `exam_school_logos.logo_media_id`, `highteen_special_classes.thumbnail_media_id`, `news.thumbnail_media_id`, `profiles.profile_image_media_id`, `teachers.profile_image_media_id`가 존재한다.
  - 본문 이미지는 Lexical upload node와 `star_cards_body_images.image_media_id` 같은 하위 relation 기준으로 관리한다.
  - 운영상 legacy path로 남겨둔 필드(`casting_appearances.thumbnail_path`, `exam_passed_reviews.student_image_path`, `exam_results.thumbnail_path`, `screen_appearances.thumbnail_path/profile_image_path`, `teachers_representative_works.poster_path`, `profiles.profile_image_path`)는 기존 데이터 fallback 또는 로컬 검수용 예외로 본다.
- [x] `public/legacy/*` staging: 로컬 검수에 필요한 legacy 이미지가 존재하고, 누락 파일은 리포트 또는 TODO로 남겼다.
  - 2026-05-27 로컬 파일 확인: `public/legacy` 아래 파일 27,478개, `public/media` 아래 파일 74,267개가 있다.
  - 주요 legacy staging 디렉터리: `teachers`, `profiles`, `news`, `artist-press`, `screen-appearances`, `direct-castings`, `casting-appearances`, `exam-passed-reviews`, `exam-passed-videos`, `exam-results`, `exam-school-logos`, `agencies`, `highteen-special-classes`, `star-cards`.
- [x] R2 object key 정책: DB에는 개발자 계정 R2 완성 URL을 저장하지 않고, object key 또는 Payload media relation 기준으로 정리했다.
  - 2026-05-27 로컬 Postgres 확인: `media` 24,881건 모두 `prefix`가 있으며, `media.url`/`media.thumbnail_u_r_l`/`media.sizes_*_url`에 남아 있던 `r2.dev` 완성 URL 866건을 `/api/media/file/{filename}?prefix={prefix}` 형태로 보정했다.
  - 보정 후 `media.url`, `media.thumbnail_u_r_l`, `media.sizes_*_url`의 `r2.dev` 포함 값은 0건이다.
  - 신규 업로드 기준: `src/lib/r2ObjectKeys.ts`의 `R2_MEDIA_PREFIX_BY_ROLE` allowlist와 `scripts/payload-migration/upload-images.ts --media-role`을 사용해 `media/{collection}/{asset_role}/...` 계층으로 올린다.
  - 본문 이미지 기준: 현재 Lexical `body` upload node가 실제 참조하는 media만 `media/{collection}/body-images/{source_id}/{role-base}-{source_id}-{index}.{ext}`로 올리고, Payload size filename은 비운다.
- [x] 레거시 fallback: 신규 필드가 비어 있어도 기존 레거시 값으로 목록/상세/SEO가 깨지지 않는다.
  - 2026-05-27 빌드 검증: `npm run typecheck`, `npm run lint`, `npm run build` 통과.
  - `news`, `artist-press`는 신규 media/SEO 필드 우선, 기존 legacy 값 fallback 구조를 유지한다.
  - 로컬 검수 화면은 `src/lib/postgresTest.ts`의 media URL 우선 처리와 legacy path fallback으로 샘플 확인 경로를 유지한다.

## 5번 진행 전 게이트

아래 항목이 모두 체크되어야 원격 Vercel Neon DB 이전을 진행한다.

- [ ] 위 테이블별 체크리스트가 모두 완료됐다.
- [x] 공통 자산 체크가 모두 완료됐다.
- [ ] `npm run typecheck`가 통과했다.
- [ ] `npm run lint`가 통과했다.
- [ ] `npm run build`가 통과했다.
- [ ] 로컬 `/test/postgres` 목록과 주요 상세 샘플을 확인했다.
- [ ] 원격 적용 전 로컬 DB 백업 또는 스냅샷을 남겼다.
- [ ] 적용할 migration 목록을 확인했다.
- [ ] 원격 명령이 `.env.local`의 Vercel Neon DB를 바라보는지 확인했다.
- [ ] 원격에 쓰기 전 현재 원격 DB 백업/복구 방법을 확인했다.

## 관련 명령

```bash
npm run legacy:db:verify
npm run payload:legacy:seed-work
npm run payload:legacy:scan-urls
npm run payload:legacy:upload-images
npm run payload:legacy:replace-image-paths
npm run typecheck
npm run lint
npm run build
```

원격 Neon 반영은 위 게이트가 끝난 뒤에만 실행한다.

```bash
npm run db:remote:migrate
```
