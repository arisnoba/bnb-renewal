# GEO 개선 후속 작업

2026-08-19 GEO 감사 후 로컬 코드만으로 안전하게 확정할 수 없는 항목을 정리합니다.

## 사용자 결정 대기

- [ ] 공개 HTML ISR 정책 결정
  - 현재 `src/app/(frontend)/layout.tsx`가 센터 판별과 점검 모드 관리자 우회를 위해 `headers()`를 읽어 모든 공개 HTML이 요청 시점에 렌더링됩니다.
  - 센터 홈의 232–306KB는 비압축 HTML이며 실제 gzip 전송량은 약 28–38KB입니다. 크기보다 `private, no-store`와 Vercel cache MISS가 핵심입니다.
  - 권장안: 일반 공개 URL은 ISR로 전환하고, 인증된 관리자 미리보기는 별도 동적 URL로 분리합니다.
  - 최소안: 점검 모드에서 관리자의 공개 화면 우회를 없애고 `/admin`에서만 관리합니다.
  - 동일 URL에서 쿠키별 응답을 유지한 채 공개 캐시 헤더만 강제로 덮는 방식은 사용하지 않습니다.

## 정확한 운영 정보 필요

- [ ] FAQ의 센터별 이관 문구 검수
  - 코드에서 확인 가능한 `02-1577-9929`와 세 종류의 `/web/html/...` 경로는 출력 시 현재 값으로 보정했습니다.
  - 입시·키즈·애비뉴 FAQ에 남은 아트센터 층수, 운영시간, 배배대본, 씨네21 안내는 각 센터의 실제 답변이 필요합니다.
  - 관리자 원문을 바꾸려면 센터별 올바른 문구와 원격 CMS 수정 승인이 별도로 필요합니다.
- [ ] 애비뉴센터 주소·전화 확인
  - 현재 코드의 애비뉴센터 주소와 좌표는 아트센터와 같습니다. 독립 주소·전화·지도 좌표가 있다면 전달받아 `src/lib/centerLocations.ts`와 CMS 푸터 정보를 함께 갱신해야 합니다.
- [ ] 시설 안내의 센터별 설명 확정
  - 현재 갤러리 제목과 설명은 모든 센터에서 아트센터 강남 사옥 문구를 사용합니다. 센터별 공간 수, 용도, 주요 설비, 접근성 문구는 클라이언트 확인 후 교체해야 합니다.

## 콘텐츠 분류 작업

- [ ] 이미지 대체텍스트 전수 분류
  - 감사 수치는 장식 이미지의 의도적인 빈 `alt`도 포함합니다.
  - 콘텐츠 이미지와 장식 이미지를 먼저 분류한 뒤, 콘텐츠 이미지에만 구체적인 대체텍스트를 입력해야 합니다. CMS 미디어 원문 변경은 별도 원격 승인 대상입니다.

## 이번 요청에서 보류한 항목

- 최상급·수치 주장에 기준일·검증 근거 추가
- 외부 권위 채널 확대
- 설명 메타를 관리자 요약 필드 대신 본문에서 생성하는 작업
- 센터 소개 본문의 고유화

뉴스 책임 정보는 화면에 추가하지 않았습니다. JSON-LD의 `Article.author`는 `센터 관리자`, `publisher`는 해당 센터 조직으로 두고, 게시일·수정일은 기존 코드 필드를 사용하도록 구현했습니다.
